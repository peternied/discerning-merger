{
    "scripts": {
        "build": "ncc build index.js -o dist"
    },
    "dependencies": {
        "@actions/core": "^1.5.0",
        "@actions/github": "^5.0.0",
        "minimatch": "^3.0.4"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.0"
    }
}
